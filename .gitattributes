# The Unix start script must keep LF endings, or it fails when checked out on
# Windows and run from Git Bash or WSL.
/gradlew        text eol=lf

# Windows scripts use CRLF.
*.bat           text eol=crlf

# Binary files are left untouched.
*.jar           binary
